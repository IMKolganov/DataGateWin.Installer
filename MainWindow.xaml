<Window x:Class="DataGateWin.Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DataGate Installer" Height="640" Width="920"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F5F6F8"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#111827"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderBrushStrong" Color="#D1D5DB"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="AccentBrushHover" Color="#1D4ED8"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushStrong}"/>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Background" Value="#E5E7EB"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentBrushHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushStrong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="DataGate Windows Installer" FontSize="22" FontWeight="SemiBold"/>
                <TextBlock Name="StepTextBlock" Text="Step 1 of 3" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Appearance" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,12,0"/>
                <RadioButton Name="LightThemeRadioButton" Content="Light" GroupName="Theme"
                             Margin="0,0,10,0" Checked="ThemeRadioButton_Checked"/>
                <RadioButton Name="DarkThemeRadioButton" Content="Dark" GroupName="Theme"
                             Checked="ThemeRadioButton_Checked"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrushStrong}"
                BorderThickness="1" CornerRadius="10" Padding="20">
            <Grid>
                <StackPanel Name="PolicyPanel" Visibility="Collapsed">
                    <TextBlock Text="License Agreement" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <Border BorderBrush="{DynamicResource BorderBrushStrong}" BorderThickness="1" CornerRadius="6" Padding="12"
                            Background="{DynamicResource WindowBackgroundBrush}">
                        <ScrollViewer Height="300" VerticalScrollBarVisibility="Auto">
                            <TextBlock TextWrapping="Wrap">
                                By installing and using DataGateWin, you agree to the terms of this software policy.
                                The software is provided &quot;as is&quot;, without warranty of any kind. You are responsible
                                for ensuring compliance with applicable laws and regulations. If you do not agree to
                                these terms, do not install or use this software.
                            </TextBlock>
                        </ScrollViewer>
                    </Border>
                    <CheckBox Name="PolicyCheckBox" Content="I accept the policy" Margin="0,12,0,0"
                              Checked="PolicyCheckBox_Changed" Unchecked="PolicyCheckBox_Changed"/>
                </StackPanel>

                <StackPanel Name="InstallPanel" Visibility="Collapsed">
                    <TextBlock Name="InstallTitleTextBlock" Text="Installing" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

                    <Grid Margin="0,0,0,0" Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Download URL:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Grid.Column="1" Name="UrlTextBox" Height="28" IsReadOnly="True" Text=""/>
                    </Grid>

                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Install folder:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Grid.Column="1" Name="InstallPathTextBox" Height="28"/>
                        <Button Grid.Column="2" Name="BrowseButton" Content="Browse..." Width="110" Height="28"
                                Margin="10,0,0,0" Click="BrowseButton_Click" Style="{StaticResource SecondaryButton}"/>
                    </Grid>

                    <TextBlock Text="Downloading" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <ProgressBar Name="DownloadProgressBar" Height="22" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,12"/>

                    <TextBlock Text="Installing" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <ProgressBar Name="InstallProgressBar" Height="22" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,12"/>

                    <TextBox Name="LogTextBox" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="12" Height="170"/>
                </StackPanel>

                <StackPanel Name="FinishPanel" Visibility="Collapsed">
                    <TextBlock Text="Setup Complete" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock Name="FinishStatusTextBlock" Text="DataGateWin has been installed on your computer."
                               TextWrapping="Wrap" Margin="0,0,0,12"/>
                    <CheckBox Name="LaunchCheckBox" Content="Launch application now" IsChecked="True"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Text="© DataGate"/>
            <Button Grid.Column="1" Name="CancelButton" Content="Cancel" Width="120" Height="32"
                    Margin="0,0,10,0" Click="CancelButton_Click" Style="{StaticResource SecondaryButton}"/>
            <Button Grid.Column="2" Name="NextButton" Content="Next" Width="120" Height="32"
                    Click="NextButton_Click" Style="{StaticResource PrimaryButton}"/>
        </Grid>
    </Grid>
</Window>
